kind create cluster --name=<cluster name> or use YAML file                       |        Creation of the cluster.
kind delete cluster --name <name of the cluster>                                 |        Deleting a cluster.
kubectl config get-contexts                                                      |        Displays the clusters(Contexts) which are present.
kubectl config use-context <name of the cluster>                                 |        Set's the context for the specified cluster.
kubectl edit pod <pod_name>                                                      |        Edit the pod it will open the editor whichever is set in the $env:KUBE_EDITOR="notepad"
kubectl get ns                                                                   |        This will give the list of all the namespaces available.
kubectl get all --namespace=<name of the ns>                                     |        Services/pods(resources) running on this namespace.
kubectl get all                                                                  |        This will give the resources running on the default namespace.
kubectl delete ns/<name of the ns>                                               |        For deleting a namespace.
kubectl create deploy <nginx-demo> --image=nginx -n <name of ns>                 |        Creating a particular resource for the specific ns.
kubectl get <resource name> -n <name of the ns>                                  |        We can monitor specific resource in the namespace.
kubectl exec -it <pod name> -n <ns name> -- sh                                   |        To interact with the pod of specific ns(namespace).
kubectl scale --replicas=3 deploy/<nginx-demo> -n=<demo>                         |        To modify any resource.
kubectl expose deploy/<nginx-demo> --name=<svc-demo> --port 80 -n=demo           |        To expose the application or services we create a nodeport service.

DAY 11
kubectl logs pods/<myapp> -c <init-myservice>                                    |        This will show the logs of the specific container in the pod
kubectl exec -it <myapp> -- printenv                                             |        This will print all the env variables inside the container.

DAY 13
kubectl get pods -n kube-system | Select-String scheduler                        |        shows the details about the static pod scheduler.
docker ps | Select-String control                                                |        This shows the control plane nodes container.
docker exec -it multi-node-cluster-control-plane sh                              |        This will take me inside the control plane container.

DAY 14
docker sbom <My-image>        |Removed|                                          |        software bill of materials, gives the details of the ingrediants of the material like this image contains this this dependecies.
docker scout sbom                                                                |        This gives the details of the dependencies along with their health information.
kubectl taint node <node name> <gpu=true:NoSchedule>                             |        To add a taint in the node.
kubectl describe node <node name> | Select-String taint                          |        Displays the taint of the node.
kubectl run redis --image=redis --dry-run=client -o yaml > redis.yaml            |        This creates a yaml file of the image quickly.
kubectl delete pods --all                                                        |        Delete all the pods at once.
kubectl taint node <node-name> <gpu=true:NoSchedule->                            |        Remove the taint from the node.
kubectl label node <node-name> <key=value(label)>                                |        Adding a label to a node.
kubectl get nodes --show-labels                                                  |        Will show all the nodes with their labels.
kubectl describe node <<nodes names>> | Select-String label                      |                "


