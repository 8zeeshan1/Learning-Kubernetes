DAY 6

Local Installation of the k8s.

Install go language in our pc as k8s built using the go lang, but that doesn't actually needed to use the k8s it's just for the learning purpose.
we will be needing a package manage choco.
We have to ensure that kubectl is installed in the system as it is the cli command of k8s.
then install the kind. it helps in running k8s with the help of the docker.

We will be using kind to install k8s in our local system.
In this installation the docker containers will be acting as the separate nodes.
So we would be needing the docker engine to be continuously running.
then we would be needing the chocolaty package manager installed in the system.

with the help of -> choco install kind --We will be intalling the kind.
then we will make the cluster using kind-> kind create cluster --image <the version required> --name <name of the cluster>
kubectl cluster-info --context kind-<name of the cluster> ==> We can see if the control plane is running and where is it running.


INTERACTION WITH K8S-->

kubectl get nodes ==> for getting the running nodes (virtual machines)
kubectl describe node <node name> ==> This will give the information about the node as the node = virtual machine we needed to see sometimes
                                      how much storage, cpu, ram is there.

<-----------------------------------------This was the installation of single node having all the pods--------------------------------->

// We want multinode cluster. NOW WE WILL BUILD THE MULTINODE CLUSTER.

So, we will have to create a YAML file for the multinode cluster.
Ideally in the practical situations we should be having more than one control plane nodes also.

after running a new cluste we saw the context is set for the newer cluster and we are not able to see the details about 
the previous cluster so for that we use the --> --set-context flag 

--------------------------------------------------- A Problem -----------------------------------------------------------------

There is a problem that developers face that is we are not able to remember all the commands of the kubernetes.
so for this we can refer to the  and search there for the cheatsheet and we will be able to 
see all the commands and their use case. https://kubernetes.io/docs/reference/kubectl/quick-reference/ this is available in the 
exam also to refer.
---------------------------------------------------------------------------------------------------------------------------------

----------------------------------------------------- A TIP ----------------------------------------------------------------------------
Remember how to switch the contexts of the clusters because in the exams we will be asked to do the task inside a particular context
so 
==> kubectl config get-contexts == This will show the current contexts.
==> kubectl config use-context <my-cluster-name> == will set the context for the particular cluster.
-----------------------------------------------------------------------------------------------------------------------------------
______________________________________________________________________________________________________________________________________________


DAY 7

CREATING A POD 

There are two ways of creating a pod 
i.  Imperative way = In this we directly write the commands. like eg: kubectl run nginx.
ii. Declarative way = In this we write a JSON or YAML file and then execute it.

i. Imperative way:
with the command kubectl run nginx-pod --image=<name of the image> --> this will run the pod and inside that pod there will be a container of 
nginx. 
One pod can have more than one containers for different different purpose.
WE can see the pods status and details with ==> kubectl get pods 
there 1/1 or 1/2 will indicate how many containers are there in the pod and how many are in running state.

ii. Declarative way:

* YAML supports different data types like List, dictionary, Integers and many mores.
* We will have to take care of indentations inside the YAML file. 

-> Ex: Adding in the dictionary in YAML:
employee:
  name:
  DOB:
  place: 

-> Ex: If we want list inside the dictionary:
employee:
  -name:zeeshan
   DOB:2005
   place:lucknow
  -name:dalle
   DOB:
   place

-> Sample of YAML Pod:

apiVersion:
kind:
metadata:
spec:

* Structure is required like this 
we can get the details about these fields with command ---> kubectl explain pod
After writting the YAML file we can run it with the help of --> kubectl create -f <name of the file>

* Can I Force Pods Onto the Control Plane?
Yes, you can. While it is highly discouraged in production environments for security and stability reasons, 
there are scenarios where it is normal:

- Single-Node Clusters: If you are using local testing tools like Minikube, Docker Desktop, 
or kind (Kubernetes in Docker), you only have one node. In these setups, the cluster provisioning tool automatically 
removes the control plane taint so that your single node can act as both the master and the worker.

- Manual Override: A cluster administrator can manually remove the taint from the control plane node using a command like:
kubectl taint nodes <node-name> node-role.kubernetes.io/control-plane:NoSchedule-

So when the pod is created scheduler sees in which worker node it has to be reside in.--> kubectl get pods -o wide ==> 
We can see on which worker node the pod has been created.

Deleting the existing pod ==> 

How to Delete a Running Pod=>
To permanently delete a pod, you use the delete command followed by the resource type and its name.
For the Nginx pod you created earlier, the command is:
==> kubectl delete pod nginx
What happens behind the scenes:
Kubernetes doesn't just instantly kill the container. It sends a "graceful termination" signal (SIGTERM) to your application, 
giving it a default of 30 seconds to finish serving current requests and shut down cleanly. If it doesn't shut down in 30 seconds, 
Kubernetes forcefully kills it (SIGKILL).

Note: Because you created this pod using kubectl run, it is an "unmanaged" pod. Once you delete it, it is gone forever. If it had 
been created by a Deployment, Kubernetes would immediately spin up a replacement to maintain the desired state.

______________________________________________________________________________________________________________________________________________

What are managed and unmanage pods?
______________________________________________________________________________________________________________________________________________

kubectl apply -f <filename> ==> This is the command to execute the YAML files.

-> HOW TO DEBUG THE IMAGE PULLING?
kubectl describe pod <pod name>
and If we have the vim editor
kubectl edit pod <pod name> --> will help us to edit the problem directly on VI editor.
-----------------------------------------------------------------------------------------------------------------------------------

How to interact with the pod:
kubectl exec -it <pod name> -- sh
-----------------------------------------------
There is also a command to just give a dry run of the command not exactly the execution but just give the dry run.
There is also a command to generate the yaml and json files of the running pod.

* NOTE == Exam will be on the cli

kubectl describe pod <podname>
* Labels in the pods are very helpful in binding them together.
______________________________________________________________________________________________________________________________________________

DAY 8
DEPLOYMENT AND REPLICATION

Replication Controller => It spins up the replicas of the pod. It helps in spinning up the pod when it goes down. It spins up multiple
identical replicas of whatever number we set to replicate. (Refer SS)
Desired number of the pods up and running and maintained by the Replication controller all the time.

Replication Controller have the ability also to spin up the new nodes, suppose the pod needs to be replicated but the resources
of the node are over then replication controller will spin up the new node and will replicate the pod in that node.

For the replication control we need to give one more entry in the spec section that is template, we will define the template for 
telling the replication controller what kind of pod we want.

spec:
  template:
    metadata:
      name: nginx-pod
      labels:
        env: demo
        type: frontend
    spec:
      containers:
      - image: nginx
        name: nginx
  replicas: 3

If we want the details about the pod of the replication controller go on and run command=>
kubectl describe pod <pod-name>

------------------------------------------------------------------------------------------------------------------------------------
Till now we saw the Replication Controller. Now we will learn about the Replica Set -->
-----------------------------------------------------------------------------------------------------------------------------------
. REPLICA SET

It is the prefferred way. The newer version.
We should use the replica set over replication controller.
With Replication controller we were only able to control the pods which were the parts of it's controller.
But with the Replica set we will be able to control the pods and replicas which are not the part of that replica set.
----------------------------------------------------------------------------------------------------------------------------------
# With the help of ==> kubectl explain <shortform of entity> it will give details how and what to write in that yaml to
create that thing.
ex: kubectl explain rs -- will explain the replica set
# group things apiVersion should be written like
<group>/<version>
-----------------------------------------------------------------------------------------------------------------------------------
